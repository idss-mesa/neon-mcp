# Deploy

Run neon-mcp as a hosted, stateless Streamable HTTP service and keep it secure.

* [Hosted HTTP deployment](hosted-http.md) - Run neon-mcp as a stateless Streamable HTTP service with uvicorn behind nginx or Caddy, systemd or Docker, health checks and scaling.
* [Security](security.md) - How neon-mcp protects NEON tokens, confines downloads, validates hosts and limits requests, and what operators must configure.
